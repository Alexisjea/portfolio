{% extends 'admin.html.twig' %}

{% block title %}About{% endblock %}

{% block body %}
    
    <h1>About</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <th>Texte</th>
                <th>Actions</th>
            </tr>
            <tr>
                <td>{{ about.id }}</td>
                
                <td>{{ about.texte }}</td>
                <td>
                    <a href="{{ path('about_show', {'id': about.id}) }}">
                        <i class="fas fa-trash mr-4"></i>
                    </a>
                    <a href="{{ path('about_edit', {'id': about.id}) }}">
                        <i class="fas fa-pen"></i>
                    </a>
                </td>

            </tr>


        </tbody>
    </table>

    <a href="{{ path('about_index') }}">back to list</a>

    <a href="{{ path('about_edit', {'id': about.id}) }}">edit</a>

    {{ include('about/_delete_form.html.twig') }}
{% endblock %}

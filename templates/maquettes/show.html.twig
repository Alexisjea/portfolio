{% extends 'admin.html.twig' %}


{% block title %}Maquettes{% endblock %}

{% block body %}
    <h1>Maquettes</h1>

<table class="table">
	<tbody>
		<tr>
			<th>Id</th>

			<th>Titre</th>

			<th>Description</th>

			<th>URL</th>

            <th>Aperçu</th>



		</tr>
		<tr>
			<td>{{ maquettes.id }}</td>
			<td>{{ maquettes.titre }}</td>
			<td>{{ maquettes.description }}</td>
            <td>{{ maquettes.url }}</td>
            <td>{{ maquettes.apercu }}</td>

			<td>
				<a href="{{ path('maquettes_delete', {'id': maquettes.id}) }}">
					<i class="fas fa-trash mr-4"></i>
				</a>
				<a href="{{ path('maquettes_edit', {'id': maquettes.id}) }}">
					<i class="fas fa-pen"></i>
				</a>
			</td>


		</tr>
	</tbody>
</table>

<a href="{{ path('maquettes_index') }}" class="mr-3">back to list</a>

<a href="{{ path('maquettes_edit', {'id': maquettes.id}) }}">edit</a>


    {{ include('maquettes/_delete_form.html.twig') }}
{% endblock %}
